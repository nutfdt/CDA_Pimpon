{% extends 'base.html.twig' %}

{% block title %}Hello FormationPimponController!{% endblock %}

{% block body %}
    <style>
        body { background: #000A5A; }
        .navbar { background-color: #212E9A; border-bottom: 3px solid white; }
        .footer { background-color: #212E9A; border-top: 3px solid white; width: 100%; height: 50px; }
        .footer p { color:#F5F5F5; text-align:center; font-size:20px; margin-top: 5px; }
        .rejoigneznous {
            background-color: #F5F5F5;
            text-align:center;
            justify-content: center;
            font-weight: bold;
            font-size: xx-large;
            margin-top: 50px;
            padding-bottom: 1px;
        }

        .rejoigneznous p {
            font-size: xx-large;
            font-weight: bold;

        }

        h2 {
            text-align: center;
            color: #F5F5F5;
            font-size: xx-large;

        }

        .card {
            margin-bottom: 25px;
        }

        .table {
            color: #F5F5F5;
        }

    </style>

    <body>
    <nav class="navbar navbar-expand-lg">
        <div class="container-fluid">
            <a class="navbar-brand" href="/"><img style="height: 60px;" src="{{ asset('img/SDPD.png') }}"/></a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
            </div>
        </div>
    </nav>

    <div class="rejoigneznous container">
        <p>Formation pompier
            <a class="btn btn-primary"
               href="{{ path('add_formpimpon') }}"
               role="button">Ajouter</a>
        </p>
    </div>

    <br>
    <h2>Liste des formations pompiers</h2>
    <br>

    <div class="container">
        <div class="row">
            {% for formationpimpon in formationspimpons %}
                <div class="col-md-4">
                    <div class="card">
                        <img src="{{ asset('img/pompier.jpg') }}"class="card-img-top" alt="...">
                        <div class="card-body">
                            <h5 class="card-title">{{ formationpimpon.idFormation.libelle }}</h5>
                            <p class="card-text">Pompier : {{ formationpimpon.idPompier.nom }} {{ formationpimpon.idPompier.prenom }}</p>
                            <p class="card-text">Date : {{ formationpimpon.date.format('Y-m-d') }}</p>
                            <p class="card-text">Durée : {{ formationpimpon.duree }}</p>
                            <p class="card-text">Adresse : {{ formationpimpon.adresse }}</p>
                            <p class="card-text">Statut : {{ formationpimpon.statut }}</p>
                            <a class="btn btn-primary"
                               href="{{ path('edit_formpimpon', {id: formationpimpon.id}) }}"
                               role="button">Edit</a>
                            <a class="btn btn-danger"
                               href="{{ path('delete_formpimpon', {id: formationpimpon.id}) }}"
                               role="button">Delete</a>
                        </div>
                    </div>
                </div>
            {% endfor %}
        </div>
    </div>

    <footer class="footer">
        <p> Copyright © 2023 Brigade de sapeurs-pompiers de Paris </p>
    </footer>
    </body>
{% endblock %}
