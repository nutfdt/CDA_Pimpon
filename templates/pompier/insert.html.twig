{% extends 'base.html.twig' %}

{% block title %}Ajout d'un Pompier{% endblock %}

{% block body %}
    <style>
        body { background: #000A5A; }
        .navbar { background-color: #212E9A; border-bottom: 3px solid white; }

        h1, h2 {
            text-align: center;
            color: #F5F5F5;
            font-size: xx-large;
        }

        .rejoigneznous {
            background-color: #F5F5F5;
            text-align:center;
            justify-content: center;
            font-weight: bold;
            font-size: xx-large;
            margin-top: 50px;
            padding-bottom: 1px;
        }

        .rejoigneznous p {
            font-size: xx-large;
            font-weight: bold;
        }

        .custom-label label {
            color: white; /* ou la couleur que vous souhaitez */
        }

        .centre {
            margin-top: 40px;
        }

    </style>

    <body>

    <nav class="navbar navbar-expand-lg">
        <div class="container-fluid">
            <a class="navbar-brand" href="/"><img style="height: 60px;" src="{{ asset('img/SDPD.png') }}"/></a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
            </div>
        </div>
    </nav>

    <div class="rejoigneznous container">
        <p>POMPIER</p>
    </div>

    <div class="centre">
        <h2>Ajout pompier</h2>

        {{ form_start(formP, { 'attr': { 'class': 'form-horizontal' } }) }}
        <div class="container">
            <div class="row justify-content-center">
                <div class="col-md-6">
                    <div class="mb-3 custom-label">
                        {{ form_label(formP.nom, 'Nom : ', {'label_attr': {'class': 'form-label'}}) }}
                        {{ form_widget(formP.nom, {'attr': {'class': 'form-control', 'required': 'required'}}) }}
                    </div>
                </div>
            </div>
            <div class="row justify-content-center">
                <div class="col-md-6">
                    <div class="mb-3 custom-label">
                        {{ form_label(formP.prenom, 'Prénom : ', {'label_attr': {'class': 'form-label'}}) }}
                        {{ form_widget(formP.prenom, {'attr': {'class': 'form-control', 'required': 'required'}}) }}
                    </div>
                </div>
            </div>
            <div class="row justify-content-center">
                <div class="col-md-6">
                    <div class="mb-3 custom-label">
                        {{ form_label(formP.grade, 'Grade : ', {'label_attr': {'class': 'form-label'}}) }}
                        {{ form_widget(formP.grade, {'attr': {'class': 'form-control', 'required': 'required'}}) }}
                    </div>
                </div>
            </div>
            <div class="row justify-content-center">
                <div class="col-md-6">
                    <div class="mb-3 custom-label">
                        {{ form_label(formP.telephone, 'Téléphone : ', {'label_attr': {'class': 'form-label'}}) }}
                        {{ form_widget(formP.telephone, {'attr': {'class': 'form-control', 'required': 'required'}}) }}
                    </div>
                </div>
            </div>
            <div class="row justify-content-center">
                <div class="col-md-6">
                    <div class="mb-3 custom-label">
                        {{ form_label(formP.adresse, 'Adresse : ', {'label_attr': {'class': 'form-label'}}) }}
                        {{ form_widget(formP.adresse, {'attr': {'class': 'form-control', 'required': 'required'}}) }}
                    </div>
                </div>
            </div>
            <div class="row justify-content-center">
                <div class="col-md-6">
                    <div class="mb-3 custom-label">
                        {{ form_label(formP.email, 'Email : ', {'label_attr': {'class': 'form-label'}}) }}
                        {{ form_widget(formP.email, {'attr': {'class': 'form-control', 'required': 'required'}}) }}
                    </div>
                </div>
            </div>
            <div class="row justify-content-center">
                <div class="col-md-6">
                    <div class="mb-3 custom-label">
                        {{ form_label(formP.idCaserne, 'Caserne : ', {'label_attr': {'class': 'form-label'}}) }}
                        {{ form_widget(formP.idCaserne, {'attr': {'class': 'form-select', 'required': 'required'}}) }}
                    </div>
                </div>
            </div>
            <div class="row justify-content-center">
                <div class="col-md-6">
                    <a class="btn btn-danger" href="{{ path('home_pompier') }}" role="button">Retour</a>
                    <button type="submit" class="btn btn-primary">Ajouter</button>
                </div>
            </div>
        </div>
        {{ form_end(formP) }}
    </div>

    <div>
        <a class="navbar-brand" href="{{ path('add_pompier') }}"><img style="height: 300px;" src="{{ asset('img/formation1.png') }}"/></a>
    </div>

    </body>
{% endblock %}
